import { BuscarEquipoPipe } from './buscar-equipo.pipe';

describe('BuscarEquipoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarEquipoPipe();
    expect(pipe).toBeTruthy();
  });
});
